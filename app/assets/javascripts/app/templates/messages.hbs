<div class="container">
  <div class="row">
    <div id="messages">
      <ul>
        {{#each message in filteredContent}}
          {{render message message}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>

{{render quoteButton}}