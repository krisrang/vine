<div class="container">
  <div class="row">
    <div id="messages">
      <ul>
        {{#each}}
          {{render message this}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>

{{render quoteButton}}